public static void main (String [] args) {
    BinaryTreeWithoutRecursion < Integer > ls = new BinaryTreeWithoutRecursion < > ();
    ls.insert (1);
    ls.insert (2);
    ls.insert (3);
    ls.insert (4);
    ls.insert (5);
    ls.insert (6);
    ls.insert (7);
    ls.inOrderPrint ();
}


  public static void main (String [] args) {
    BinaryTreeWithoutRecursion < Integer > ls = new BinaryTreeWithoutRecursion < > ();
    ls.performProcess (1);
    ls.performProcess (2);
    ls.performProcess (3);
    ls.performProcess (4);
    ls.performProcess (5);
    ls.performProcess (6);
    ls.performProcess (7);
    ls.printInOrderWithoutRecursion ();
}
